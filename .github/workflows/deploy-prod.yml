# ───────────────────────────────────────────────────────────────────
#  Deploy Production — Juancho's Pizza / GastroPro
#  Pipeline seguro para desplegar Docker Compose en VPS via SSH.
#
#  TRIGGERS:
#    - workflow_dispatch (manual, con selector de rama)
#    - push a master (auto-deploy: cada push a master despliega)
#
#  REQUISITOS (GitHub Secrets):
#    PROD_HOST       — IP del VPS
#    PROD_USER       — usuario SSH (ej. deploy)
#    PROD_SSH_KEY    — clave privada SSH
#    PROD_PATH       — ruta absoluta al proyecto (ej. /opt/guido-pizza)
#
#  FLUJO:
#    1. Quality: typecheck + build + tests
#    2. Docker build check (opcional, verifica que la imagen compile)
#    3. Deploy: git pull → docker compose up -d --build → healthcheck
# ───────────────────────────────────────────────────────────────────

name: 🚀 Deploy Production (Docker Compose)

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Rama a desplegar'
        required: true
        default: 'master'
        type: choice
        options:
          - master
          - main

  # Auto-deploy: cada push a master lanza quality → build → deploy
  push:
    branches: [master, main]

concurrency:
  group: deploy-production-${{ github.ref }}
  cancel-in-progress: false  # No cancelar un deploy en curso

env:
  NODE_VERSION: '20'

jobs:
  # ── JOB 1: Quality Gate ────────────────────────────────────────
  quality:
    name: 🔍 Quality (typecheck · build · test)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      DATABASE_URL: postgres://ci:ci@localhost:5432/ci
      JWT_SECRET: ci-secret-no-usar-en-prod
      NODE_ENV: test

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch || github.ref_name }}

      - name: 🟢 Setup Node ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 Install
        run: npm ci --no-audit --no-fund

      - name: 🔷 TypeScript Check
        run: npx tsc --noEmit

      - name: 🏗️ Build
        run: npx vite build

      - name: 🧪 Unit Tests
        run: npx vitest run --reporter=default

      - name: 🛡️ Dependency Audit
        continue-on-error: true
        run: npm audit --audit-level=high --omit=dev

      - name: 🔍 Lint
        continue-on-error: true
        run: npx eslint . --ext .ts,.tsx,.js --quiet

  # ── JOB 2: Docker Build Check ─────────────────────────────────
  docker-check:
    name: 🐳 Docker Build Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [quality]

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch || github.ref_name }}

      - name: 🐳 Build Docker image
        run: docker build -t guidopizza:${{ github.sha }} .

      - name: 🐳 Verify image
        run: |
          echo "✅ Docker image built: guidopizza:${{ github.sha }}"
          docker images guidopizza --format '{{.Repository}}:{{.Tag}} ({{.Size}})'

  # ── JOB 3: Deploy via SSH ─────────────────────────────────────
  deploy:
    name: 🚀 Deploy to VPS
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [quality, docker-check]
    # Corre con ambos triggers: workflow_dispatch manual y push a master.
    # En push, github.event.inputs.branch es vacío y BRANCH cae al default
    # 'master' dentro del script SSH (ver paso 0).
    environment: production

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch || github.ref_name }}

      - name: 🔑 Setup SSH and Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.PROD_HOST }}
          username: ${{ secrets.PROD_USER }}
          key: ${{ secrets.PROD_SSH_KEY }}
          # Fingerprint (SHA256) de la host key del VPS: appleboy/ssh-action
          # (vía easyssh-proxy) compara ssh.FingerprintSHA256 de la key que el
          # SERVIDOR elige presentar en el handshake con el cliente Go — NO es
          # la de ssh-keyscan local (que lista TODAS las keys del server).
          # Verificado 2026-08-14 (keyscan desde el runner = mismas keys):
          #   ed25519 = SHA256:51L6hCFxpqMkxlboJa5Y3EIOg59U+V6Wy7lIzKIw/8g ✗
          #   rsa     = SHA256:ntiGLnJqkfc3452w5P5kRYlG+mEcL06R2hjN9iThZTU ✗
          #   ecdsa   = SHA256:970NxLNqTjUqnjEsrzV09GaCHAgWPcKCffzwzb+Mj9E ✓
          # El server presenta ECDSA al cliente Go (ssh-rsa quedó deprecado
          # y Go no lo ofrece como host key algorithm en versiones recientes).
          fingerprint: SHA256:970NxLNqTjUqnjEsrzV09GaCHAgWPcKCffzwzb+Mj9E
          # Tiempo suficiente para docker compose pull + build + up
          command_timeout: 300s
          script: |
            set -e
            echo "🚀 Iniciando deploy en $(hostname)"

            # ── 0. Valores con fallback en shell ──────────────
            # ⚠️ NO usar literales con comillas dentro de expresiones de
            # GitHub Actions en bloques run/script: el parser rompe el
            # YAML del workflow. Los defaults se resuelven acá en shell.
            PROJECT_DIR="${{ secrets.PROD_PATH }}"
            PROJECT_DIR="${PROJECT_DIR:-/opt/guido-pizza}"
            BRANCH="${{ github.event.inputs.branch }}"
            BRANCH="${BRANCH:-master}"

            # ── 1. Ir al proyecto y traer últimos cambios ──────
            cd "$PROJECT_DIR"
            echo "📥 Pulling branch: $BRANCH"
            git fetch origin
            git checkout "$BRANCH"
            git pull origin "$BRANCH"

            # ── 2. Copiar .env.production desde backup ────────
            # Asume que .env.production existe en el servidor
            # (se creó manualmente en el primer deploy)
            if [ ! -f .env.production ]; then
              echo "❌ .env.production no encontrado en $PROJECT_DIR"
              echo "   Copiar desde backup o crear manualmente"
              exit 1
            fi

            # ── 3. Backup previo obligatorio ──────────────────
            # El runner de GitHub no puede alcanzar postgres:5432 porque
            # solo está expuesto dentro de app-network. El backup se ejecuta
            # aquí, en el VPS, antes de bajar el stack.
            echo "📦 Creando backup previo al deploy..."
            BACKUP_DIR="$PROJECT_DIR/backups"
            mkdir -p "$BACKUP_DIR"
            BACKUP_FILE="$BACKUP_DIR/guido-pizza_predeploy_$(date +%Y%m%d_%H%M%S).dump"
            # Asegurar que PostgreSQL esté disponible sin tocar app/nginx.
            docker compose --env-file .env.production up -d postgres
            docker compose --env-file .env.production exec -T postgres pg_isready -U postgres -d juanchos_pizza
            docker compose --env-file .env.production exec -T postgres \
              pg_dump -U postgres -d juanchos_pizza -Fc -Z 9 > "$BACKUP_FILE"
            test -s "$BACKUP_FILE"
            sha256sum "$BACKUP_FILE" > "$BACKUP_FILE.sha256"
            find "$BACKUP_DIR" -name 'guido-pizza_*.dump' -type f -mtime +30 -delete
            find "$BACKUP_DIR" -name 'guido-pizza_*.dump.sha256' -type f -mtime +30 -delete
            echo "✅ Backup previo creado: $BACKUP_FILE"

            # ── 4. Build + Deploy con Docker Compose ──────────
            echo "🐳 Building and starting containers..."
            # down + up --build es más limpio que restart
            docker compose --env-file .env.production down --remove-orphans
            docker compose --env-file .env.production up -d --build

            # ── 5. Limpiar imágenes viejas ────────────────────
            echo "🧹 Pruning old Docker images..."
            docker image prune -f 2>&1 || true

            # ── 6. Health check obligatorio ───────────────────
            echo "⏳ Esperando health check (30s)..."
            sleep 30

            # Intentar health check directo al app, fallback vía nginx.
            HEALTH=$(curl -fsS -o /dev/null -w "%{http_code}" http://localhost:3001/api/health 2>/dev/null || true)
            if [ "$HEALTH" != "200" ]; then
              HEALTH=$(curl -kfsS -o /dev/null -w "%{http_code}" https://localhost/api/health 2>/dev/null || true)
            fi

            if [ "$HEALTH" != "200" ]; then
              echo "❌ Health check falló (HTTP ${HEALTH:-000}) — rollback manual requerido"
              docker compose ps
              docker compose logs --tail=50 app nginx
              exit 1
            fi
            echo "✅ App health check OK"
            docker compose --env-file .env.production exec -T postgres pg_isready -U postgres -d juanchos_pizza
            docker compose --env-file .env.production exec -T redis redis-cli ping | grep -qx PONG
            docker compose --env-file .env.production exec -T nginx nginx -t
            echo "✅ App, PostgreSQL, Redis y Nginx verificados"

            # ── 7. Estado final ───────────────────────────────
            echo ""
            echo "📊 Container status:"
            docker compose ps
            echo ""
            echo "✅ Deploy completado: $(date '+%Y-%m-%d %H:%M:%S %Z')"

      - name: 📬 Post-deploy notification
        if: always()
        run: |
          if [ "${{ job.status }}" = "success" ]; then
            echo "✅ Deploy completado exitosamente"
          else
            echo "❌ Deploy falló — revisar logs de VPS"
          fi

  # ── JOB 4: Smoke Test Post-Deploy ─────────────────────────────
  smoke:
    name: 🔥 Smoke Test
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [deploy]
    if: success()

    steps:
      - uses: actions/checkout@v4

      - name: 🟢 Setup Node ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 Install
        run: npm ci --no-audit --no-fund

      - name: 📦 Install Playwright
        run: npx playwright install chromium --with-deps

      # El E2E apunta al dominio real en producción.
      # Config dedicado (e2e/playwright.smoke.config.ts): sin webServer local
      # (antes arrancaba dev:all, sin postgres en el runner → timeout de 90s
      # y el `|| echo` ocultaba el fallo: job verde falso). Ahora el job FALLA
      # si el smoke no pasa.
      - name: 🌐 E2E Smoke contra producción
        run: |
          npx playwright test --config=e2e/playwright.smoke.config.ts
        env:
          PROD_URL: ${{ secrets.PROD_URL }}
